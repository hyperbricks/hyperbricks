# Optional API and guard lessons. Start the local fixture first; see docs/advanced.md.
hyperbricks:
  mode: development
  metadata:
    module: hyperbricks-basics
    moduleversion: "1.0.0"
  development:
    watch: true
    watch_dirs:
      - hyperbricks
      - templates
      - resources
      - path: {base: module, path: hyperbricks}
      - path: {base: module, path: lessons}
    reload: true
    frontend_errors: false
    dashboard: false
  server:
    port: 8104
    beautify: true
  rate_limit:
    enabled: true
    requests_per_second: 100
    burst: 200
  plugins:
    enabled: []
  directories:
    hyperbricks:
      path: {base: module, path: lessons/advanced}
    templates:
      path: {base: module, path: templates}
    resources:
      path: {base: module, path: resources}
    static:
      path: {base: module, path: static}
    render:
      path: {base: module, path: rendered}
    plugins: ./bin/plugins
